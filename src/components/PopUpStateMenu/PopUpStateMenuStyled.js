import { Menu, MenuItem } from '@material-ui/core';
import styled from 'styled-components';

export const PopUpMenu = styled(Menu)``;
export const PopUpMenuItem = styled(MenuItem)`
  outline: 1px solid red;
`;
